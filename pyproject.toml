[project]
name = "ecnu-api-codex-skill"
version = "0.1.0"
description = "Unofficial Codex skill for the ECNU LLM Open Platform API."
readme = "README.md"
requires-python = ">=3.10"
license = "MIT"

[dependency-groups]
dev = [
    "pyyaml>=6.0.2",
]
